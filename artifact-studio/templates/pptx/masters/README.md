# PPTX masters

Slide masters are defined as code in `src/renderers/pptx/` so theme tokens,
geometry and semantic layout selection remain testable. This directory is
reserved for audited OOXML reference decks if a future corporate theme needs
one; full-slide image templates are not accepted.
