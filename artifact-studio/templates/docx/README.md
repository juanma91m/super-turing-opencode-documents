# DOCX templates

Artifact Studio uses `docx.js` by default. Place only audited `.docx` templates
here and register their data contract before enabling the optional
Docxtemplater adapter. `DOCX_ENGINE=auto` never requires PRO modules and falls
back to the programmatic renderer.
