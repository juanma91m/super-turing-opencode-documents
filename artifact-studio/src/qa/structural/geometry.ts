export interface Box { id: string; x: number; y: number; w: number; h: number; allowOverlap?: boolean; }
export interface GeometryIssue { kind: "OUT_OF_BOUNDS" | "SAFE_ZONE" | "OVERLAP"; elements: string[]; }
export function validateGeometry(boxes: Box[], width = 13.333, height = 7.5, safe = 0.2): GeometryIssue[] { const issues: GeometryIssue[] = []; for (const b of boxes) { if (b.x < 0 || b.y < 0 || b.x + b.w > width || b.y + b.h > height) issues.push({ kind: "OUT_OF_BOUNDS", elements: [b.id] }); else if (b.x < safe || b.y < safe || b.x + b.w > width - safe || b.y + b.h > height - safe) issues.push({ kind: "SAFE_ZONE", elements: [b.id] }); } for (let i = 0; i < boxes.length; i++) for (let j = i + 1; j < boxes.length; j++) { const a = boxes[i], b = boxes[j]; if (!a.allowOverlap && !b.allowOverlap && a.x < b.x + b.w && a.x + a.w > b.x && a.y < b.y + b.h && a.y + a.h > b.y) issues.push({ kind: "OVERLAP", elements: [a.id, b.id] }); } return issues; }
