Usá el agente `documenter` para ejecutar una prueba end-to-end del spec
`artifact-studio/examples/end-to-end/incident-service-flow/spec.json`.

Renderizá con `artifact-service-flow` en formato `all` bajo
`artifact-studio/artifacts/work/end-to-end/incident-service-flow/`, con basename
`incident-response`. Verificá receipt y spec, ejecutá `artifact-validate` sobre
el SVG e inspeccioná PNG y PDF. Prestá atención a que la secuencia sea legible
sin conectores y no agregues flechas ni información no presente en el spec.

Entregá paths, checks, browser QA, findings visuales y limitaciones.
