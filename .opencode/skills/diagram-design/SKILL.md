---
name: diagram-design
description: Use when converting architecture, infrastructure, process, sequence or dependency information into a D2 diagram.
---
# Diagram design

Use D2 as the primary renderer and SVG as the canonical output. Keep nodes concise, group related components, minimize crossings and move explanatory prose outside the diagram. Graphviz is a secondary option for dense graphs; Mermaid is interoperability fallback only.
