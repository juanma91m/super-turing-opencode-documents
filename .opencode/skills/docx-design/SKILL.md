---
name: docx-design
description: Use when generating or reviewing an editable Word DOCX with real styles, sections, tables, figures, headers and footers.
---
# DOCX design

Default to the programmatic docx.js renderer. Use the template adapter only with an explicit audited Word template; `DOCX_ENGINE=auto` must fall back safely when premium modules are absent. Keep all text and tables editable and use named Word styles rather than screenshots.
