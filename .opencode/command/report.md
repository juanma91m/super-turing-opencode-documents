---
description: Generate and visually review a professional Typst PDF report.
agent: documenter
---
Generate a professional PDF report for: $ARGUMENTS

Follow `artifact-generation`, `report-design`, `source-integrity` and the full validation/visual-QA cycle. Keep the final PDF in `artifact-studio/artifacts/output/` or the requesting project's `artifacts/output/`.
